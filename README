Installation

This will compile and install the files in your /usr/lib and /usr/include directories.

	make
	make install

Usage

Include the following librairy :

	<libhex.h>

Use the flag -lhex to make a link with the librairy.

